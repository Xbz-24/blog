import React from 'react';

const LoadingSpinner = () => {
  return (
    <div>
      <h3>Loading...</h3>
      {/* You can use CSS to add a spinning animation to the loading indicator */}
    </div>
  );
};

export default LoadingSpinner;
